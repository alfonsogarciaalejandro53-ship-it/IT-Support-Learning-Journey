# ARP
## ¿ Que es ?
ARP ( Address Resolution Protocol) es un protocolo que permite obtener la direccion MAC asociada con una direccion IP dentro de una red local
## Ejemplo
Si una PC necesita enviar datos al router, primero debe conocer la direccion MAC del Router.
Para ello envia una solicitud ARP y el Router responde con su direccion MAC.
